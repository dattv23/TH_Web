<head>
    <style>
        #order-completed {
            border-radius: 40px;
            text-align: center;
            background-color: #fff;
            box-shadow: 0px 0px 15px 11px rgba(0,0,0,0.1);
        }

        .fa-regular {
            background-image: linear-gradient(to right, #43e97b 0%, #38f9d7 100%);
            color: transparent;
            background-clip: text;
            -webkit-background-clip: text;
        }

        .fa-circle-check {
            margin-top: 20px;
            font-size: 150px;
        }

        .text-main {
            font-size: 35px;
            color: #14843a;
            font-weight: 500;
            margin-top: 50px;
        }

        .text {
            margin-top: 15px;
        }

        #btn-return {
            display: inline-block;
            flex: 1 1 auto;
            margin: 10px;
            margin-top: 70px;
            padding: 30px;
            text-align: center;
            text-transform: uppercase;
            transition: 0.5s;
            background-size: 200% auto;
            color: white;
            text-shadow: 0px 0px 10px rgba(0,0,0,0.2);
            box-shadow: 0 0 20px #eee;
            border-radius: 10px;
        }

        #btn-return {
            text-decoration: none;
        }

            #btn-return:hover {
                background-position: right center;
                cursor: pointer;
            }

        .btn-3 {
            background-image: linear-gradient(to right, #84fab0 0%, #8fd3f4 51%, #84fab0 100%);
        }
    </style>
</head>

<div id="order-completed">
    <i class="fa-regular fa-circle-check"></i>
    <div class="text-main">Whohoo!</div>
    <div class="text-main text">
        Đặt hàng thành công
        <i class="fa-regular fa-face-smile-beam"></i>
    </div>
    <div>
        <a href="@Url.Action("Index", "Home")" id="btn-return" class="btn btn-3">Tiếp tục mua sắm</a>
    </div>
</div>

